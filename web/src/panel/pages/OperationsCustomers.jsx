import { useEffect,useMemo,useState } from 'react'
import { Link } from 'react-router-dom'
import { customers } from '../lib/operations-store.js'
export default function OperationsCustomers(){const [rows,setRows]=useState([]),[q,setQ]=useState('');useEffect(()=>{customers.list().then(setRows)},[]);const filtered=useMemo(()=>rows.filter((r)=>!q||`${r.title||''} ${r.contact||''} ${r.email||''}`.toLocaleLowerCase('tr').includes(q.toLocaleLowerCase('tr'))),[rows,q]);return <div><div className="pl-head"><div><h1>Müşteriler</h1><p>Operasyon kayıtlarına erişim.</p></div></div><div className="pl-toolbar"><input type="search" value={q} onChange={(e)=>setQ(e.target.value)} placeholder="Müşteri ara…"/></div><div className="pl-card"><div className="pl-card-b">{filtered.length===0&&<div className="pl-empty">Kayıt yok.</div>}{filtered.map((c)=><div className="pl-row" key={c.id}><div className="grow"><div className="t1">{c.title}</div><div className="t2">{c.contact||'—'} · {c.status||'—'}</div></div><Link className="pl-btn pl-btn-ghost pl-btn-sm" to={`/panel/musteriler/${c.id}`}>Operasyon kaydı →</Link></div>)}</div></div></div>}
